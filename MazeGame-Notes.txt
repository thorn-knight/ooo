
Run local server py -m http.server

Old sprite size: 32 x 42 without background.

New sprite size : 64 x 84

New map width = 12, height = 11 (tiles)
this is: width = 768, height = 924

For tilesets spacing between seems to be 4px

Process to convert animations to new maze game in Krita.
    1. Start with spritesheet from old maze game resources folder, open it in krita.
    2. In krita, file -> new. 32x42 dimensions.
    3. Rectangle select the image from the spritesheet and copy it.
    4. In new file, paste or do ctrl + alt + v to paste at cursor (useful if it gets cut off on paste). Save the new file (as PNG). Rectangle select it and move it if needed.
    5. Close and re-open the file. Pick a color to be the background and use the fill tool (paint bucket thing) to fill it everywhere there's background.
    6. Select the similar color selection tool and click on the background color. Press delete key, this should remove all the background.
    7. Close and re-open the file. Image -> scale image to new size. Do dimensions 64x84 and save it.
    8. Rinse and repeat for each image in the spritesheet.
    9. Once done use free texture packer to re-pack everything into a texture png + json file. IMPORTANT - select JSON (array) as the format for melon.


https://unpkg.com/browse/
https://cdn.jsdelivr.net/npm/javascript-astar@0.4.1/astar.min.js


Roadmap forward to do:
1. Implement collision detection a bit better. I'm thinking calculate distance of center of sprites and only consider it a collision when its within a certain distance in onCollision. Maybe can throw this in a util. UPDATE possibly done with latest updates checking game.WORLD_SHAPE_COLLISION_OVERLAP_THRESHOLD and game.ENTITY_COLLISION_OVERLAP_THRESHOLD? Need to apply this to other collisions types but seems to work? May need to tighten the value but it looks like 'actual collision' needs to happen and we can't be killed through walls/adjacent tiles anymore.
2. Extra maps, load random one on new level.
3. On title screen a how to play button/screen.

Nice to have: 
1. Extra lives. This might make the game a bit too easy, just an idea for now.
2. Enemy behavior modes? Regular vs aggro?
3. Homing missile behavior/animation looks a bit wonky, see if this can be cleaned up.
4. General code clean up, remove magic numbers in hud, etc.

original melon 17 import map entry for reference: "melonjs": "https://cdn.jsdelivr.net/npm/melonjs@17/+esm",
previous melon import map: "melonjs": "https://cdn.jsdelivr.net/npm/melonjs@19.7.1/+esm",

